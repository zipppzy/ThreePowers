# [[locations]]
# name = "Location Name"                 # Required: Display name
# visibleToPlayer = true                 # Required: Whether player can see it
# defaultHidden = false                  # Optional: Start hidden (default: false)
# defaultLocked = false                  # Optional: Start locked (default: false)
# actions = ["Action1", "Action2"]       # Optional: Available action names
# subLocationNames = ["SubLoc1"]         # Optional: Child location names

[[locations]]
name = "Village"
actions = ["Meditate", "Beg", "Look for work"]
subLocationNames = ["Field", "Farm"]

[[locations]]
name = "Field"
actions = ["Run", "Push Ups"]

[[locations]]
name = "Farm"
defaultHidden = true
actions = ["Farm Work"]